# What port to listen on
PORT=8888

# Maximum total number of files the server will store (all clients combined)
MAX_NUM_FILES=16

# Maximum file size that the server will allow
MAX_FILESIZE_MB=8

# Maximum number of files each client can upload per minute
MAX_USER_UPLOADS_PER_MINUTE=4
